import React from 'react';

import './styles.css';

// criando um compoente por função constante em vez de uma classe
const Header = () => (
  <header id="main-header">JSHunt</header>
);

export default Header;
// Usamos classe para o uso de State